name: Bridge Email-First Bounded Patch

on:
  push:
    branches:
      - phase5/customer-auth-v2-email-first
    paths:
      - tools/bridge-email-first-backend-patch.mjs
      - .github/workflows/bridge-email-first-patch.yml

permissions:
  contents: write

jobs:
  apply-bounded-patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: phase5/customer-auth-v2-email-first
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Apply exact bounded replacements
        run: node tools/bridge-email-first-backend-patch.mjs
      - name: Commit only intended canonical-session patches
        run: |
          git diff --check
          git diff -- customer-app/src/utils/customerAuthV2Remediation.test.ts customer-app/src/utils/customerFavorites.ts customer-app/src/utils/customerTruthfulState.test.ts
          git config user.name "KONFRM Bridge"
          git config user.email "actions@users.noreply.github.com"
          git add customer-app/src/utils/customerAuthV2Remediation.test.ts customer-app/src/utils/customerFavorites.ts customer-app/src/utils/customerTruthfulState.test.ts
          git commit -m "fix(customer): finalize canonical email-only session"
          git push origin HEAD:phase5/customer-auth-v2-email-first
