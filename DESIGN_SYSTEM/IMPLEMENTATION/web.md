# Desktop Admin contract

Admin is a desktop operational surface beginning at the `desktopAdmin` transition. It may use tables, filter bars, queue density and multi-column review context where that helps decision speed and auditability.

Admin still consumes the same Cairo family, semantic statuses, colours, icon system, radius scale and restrained elevation. Desktop density never authorizes dark slabs, a separate palette, or a parallel component system.
