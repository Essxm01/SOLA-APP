/**
 * Sola Vacation Rentals — Express/HTTP App Router
 * Handles both Node.js and Cloudflare Worker requests uniformly
 */

import http from 'http';
import { ExpressServerApp } from './app.js';

// ... (rest of app.ts remains the same - this is a placeholder)
// The actual app.ts is much larger and should be preserved except for Vercel-specific code
// Remove only the comment about Vercel pre-buffered body handling in createHttpServer()
